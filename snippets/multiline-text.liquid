{%- liquid
  assign split_text = multiline-text | newline_to_br | split: '<br />'
  for line in split_text
    unless forloop.first
      echo '<br />'
    endunless
    echo line | escape
  endfor
-%}
